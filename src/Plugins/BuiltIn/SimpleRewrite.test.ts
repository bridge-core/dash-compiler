import { describe, test, expect } from 'vitest'
import { SimpleRewrite } from './SimpleRewrite'

// test('[PROD MODE] BP File', () => {
// 	const plugin = SimpleRewrite({ options: {} })

// 	expect(plugin.transformPath).toBeDefined()

// 	expect(plugin.transformPath?.('BP/test.txt')).toBe(
// 		'builds/dist/BP/test.txt'
// 	)
// })
